<div class="min-h-full"></div>
<header class="bg-slate-100 shadow-md">
    <div class="flex justify-between max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
        <a href="/">
            <img src="/images/logo.svg" class="h-8" alt="Authui Logo" />
        </a>
        <h1 class="text-3xl font-bold tracking-tight text-gray-900">{{ user.username }} Settings</h1>
    </div>
</header>

<main>
    <div class="p-4">
        <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
            <form action="/projects/update-settings/" method="post" class="space-y-6">


                {{!-- <div class="bg-white p-6 rounded-lg shadow-md">
                    <div class="w-full mr-4">
                        <h2 class="text-xl font-semibold text-gray-900 mb-4">Login
                            Callback URL</h2>
                        <div class="flex justify-between space-x-3 items-center">
                            <input type="url" id="callback-url" name="callback_url"
                                class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                                placeholder="https://example.com/callback" required />
                            <a href="/projects/login-forms/{{project.id}}/"
                                class="px-4 py-2 shadow-lg text-nowrap text-white shadow-slate-400 bg-slate-900 text-center rounded-md">Select
                                Form</a>
                        </div>

                    </div>
                </div> --}}

                {{!-- <div class="bg-white p-6 rounded-lg shadow-md">
                    <div class="w-full mr-4">
                        <h2 class="text-xl font-semibold text-gray-900 mb-4">Signup
                            Callback URL</h2>
                        <div class="flex justify-between space-x-3 items-center">
                            <input type="url" id="callback-url" name="callback_url"
                                class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                                placeholder="https://example.com/callback" required />
                            <a href="/projects/signup-forms/{{project.id}}/"
                                class="px-4 py-2 shadow-lg text-nowrap text-white shadow-slate-400 bg-slate-900 text-center rounded-md">Select
                                Form</a>
                        </div>

                    </div>


                </div> --}}


                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h2 class="text-xl font-semibold text-gray-900 mb-4">Public Api Key
                    </h2>
                    <div class="space-y-4">
                        <div>
                            <label for="setting-1" class="block text-sm mb-1 font-medium text-gray-700">Your Api
                                Key</label>
                            <div
                                class="flex justify-between space-x-3 outline rounded-md outline-2 px-3 outline-gray-400 items-center">
                                <input readonly type="text" id="callback-url" value="{{apiKey.api_secret_key}}"
                                    class="mt-1 block w-full border-none px-3 py-2 shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                                    required />
                                <a href="/accounts/get-api-Key/"
                                    class="px-4 py-1 text-nowrap text-slate-500 bg-slate-200 text-center rounded-md">Copy</a>
                            </div>

                        </div>
                    </div>
                </div>



                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h2 class="text-xl font-semibold text-gray-900 mb-4">Private Api Key
                    </h2>
                    <div class="space-y-4">
                        <div>
                            <label for="setting-1" class="block text-sm font-medium text-gray-700">Your Api
                                Key</label>
                            <div class="flex justify-between space-x-3 items-center">
                                <input readonly type="text" id="callback-url" value="{{apiKey.private_key}}"
                                    class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                                    required />
                                <a href="/accounts/get-api-Key/"
                                    class="px-4 py-2 shadow-lg text-nowrap text-white shadow-slate-400 bg-slate-900 text-center rounded-md">Generate
                                    Key</a>
                            </div>

                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</main>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const prevBtn =
            document.getElementById('prev-btn'); const nextBtn =
                document.getElementById('next-btn'); const carousel =
                    document.getElementById('carousel'); let scrollAmount = 0; const scrollStep
                        = 200; function updateButtons() {
                            const maxScrollLeft = carousel.scrollWidth
                                - carousel.clientWidth; prevBtn.disabled = scrollAmount <= 0;
                            nextBtn.disabled = scrollAmount >= maxScrollLeft;
                        }
        prevBtn.addEventListener('click', () => {
            scrollAmount =
                Math.max(scrollAmount - scrollStep, 0); carousel.scrollTo({
                    left:
                        scrollAmount, behavior: 'smooth'
                }); updateButtons();
        });
        nextBtn.addEventListener('click', () => {
            scrollAmount =
                Math.min(scrollAmount + scrollStep, carousel.scrollWidth -
                    carousel.clientWidth); carousel.scrollTo({
                        left: scrollAmount, behavior:
                            'smooth'
                    }); updateButtons();
        }); // Initial button state updateButtons();
    });
</script>
</div>