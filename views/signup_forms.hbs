<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select a Form for Projects</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.0.24/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <div class="min-h-full">
        <header class="bg-white shadow">
            <div class="mx-auto flex justify-between max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
                <h1 class="text-xl font-bold tracking-tight text-gray-900">Select a Signup Form for Your Project</h1>
                <a class="text-slate-900 hover:underline" href="/projects/login-forms/{{project_id}}/">Login Forms</a>
            </div>
        </header>

        <main>
            <div class="p-6">
                <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
                    <!-- Form Card 1 -->
                    {{#each forms}}
                    <div class="bg-white shadow-lg rounded-lg overflow-hidden">
                        <img class="w-full h-32 object-cover" src="/images/form-preview1.jpg" alt="Form Preview 1" />
                        <form class="p-4" method="POST" action="/projects/select-project-signup-form">
                            <h2 class="text-xl font-semibold text-gray-800">Form Title 1</h2>
                            <p class="mt-2 text-gray-600">Brief description of Form 1. This is where you can give users
                                an idea of what the form is about.</p>
                            <input type="hidden" name="project_id" value="{{ project_id }}" />
                            <input type="hidden" name="signup_form_id" value="{{this.id}}" />
                            <button type="submit"
                                class="mt-4 block text-center w-full text-white bg-gray-900 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500 font-medium rounded-lg px-4 py-2">Select
                                Form</button>
                        </form>
                    </div>
                    {{/each}}
                </div>
            </div>
        </main>
    </div>
</body>

</html>